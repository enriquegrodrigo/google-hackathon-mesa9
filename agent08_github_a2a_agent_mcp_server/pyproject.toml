
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gitHub_a2a_agent"
version = "0.1.0"
authors = [
    { name = "Arnaud Legendre", email = "legendre@google.com" }
]
requires-python = ">=3.13"

dependencies = [
  "google-adk[a2a]==1.18.0",
]
